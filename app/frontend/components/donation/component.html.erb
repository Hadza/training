<%= link_to @donation, class: "card shadow-none border-solid border border-gray-6 rounded p-6 mb-6" do %>
  <div class="flex justify-between mb-3">
    <div class="text-xs text-gray-4">
      <%= "Posted: #{creation_date}" %>
    </div>
    <div class="<%= claimed_status_class %>">
      <%= claimed_status %>
    </div>
  </div>
  <h4 class="text-gray-3 mb-3">
    <%= @donation.name %>
  </h4>
  <div class="text-gray-4 text-xs mb-2">
    <%= "Category: #{@donation.category.name}" %>
  </div>
  <div class="text-gray-4 text-xs">
    <%= "Posted by: #{@donation.donor.email}" %>
  </div>
<% end %>
